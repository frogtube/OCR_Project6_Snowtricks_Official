{% block file_widget %}
    {% spaceless %}

        {% if video_url is not null %}
            <iframe width="420" height="315"
                    src="{{ asset(video.embed) }}">
            </iframe>
        {% endif %}

    {% endspaceless %}
{% endblock %}