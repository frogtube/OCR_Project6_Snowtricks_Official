{{ form_start(form) }}
        {{ form_errors(form) }}

        {{ form_row(form.content, {
            'label': 'Votre commentaire'
        }) }}

<button type="submit" formnovalidate>Envoyer mon commentaire</button>

{{ form_end(form) }}